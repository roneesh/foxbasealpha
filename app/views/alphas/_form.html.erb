<%= form_for(@alpha) do |f| %>
  <% if @alpha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alpha.errors.count, "error") %> prohibited this alpha from being saved:</h2>

      <ul>
      <% @alpha.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label(:public, "Alpha is Public") %>
    <%= f.radio_button(:public, true) %>
    <%= f.label(:public, "Alpha is Protected") %>
    <%= f.radio_button(:public, false) %>
    

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
